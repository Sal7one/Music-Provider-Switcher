<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sal7one.musicswitcher">
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <application
        android:allowBackup="false"
        android:icon="@mipmap/t_switcher_launcher_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/t_switcher_launcher_icon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MusicSwitcher">
        <activity
            android:name=".DeepLinkHandlerActivity"
            android:exported="true">
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:host="open.spotify.com"  android:scheme="https" />
                <data android:host="open.spotify.com" android:scheme="http" />

                <data android:host="music.apple.com" android:scheme="https" />
                <data android:host="music.apple.com" android:scheme="http" />

                <data android:host="play.anghami.com"  android:scheme="https" />
                <data android:host="play.anghami.com" android:scheme="http" />

                <data android:host="www.deezer.com"  android:scheme="https" />
                <data android:host="www.deezer.com" android:scheme="http" />

                <data android:host="music.youtube.com"  android:scheme="https" />
                <data android:host="music.youtube.com" android:scheme="http" />
            </intent-filter>


        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>